import 'package:flutter/material.dart';

class Mapeventdatetime {
  DateTime from;
  DateTime to;
  String location;
  Mapeventdatetime(
      {required this.from, required this.location, required this.to});
}
